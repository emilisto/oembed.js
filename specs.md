Adds <oembed /> tags in the HTML code.

Include `oembed.js` as a shim in the <head> - will load all oembeds in
body.

Register custom representations for different types: 
	- photo
	- video
	- link
	- rich
